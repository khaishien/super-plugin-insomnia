export enum IInsomniaDataType {
  Workspace = 'Workspace',
  Request = 'Request',
  RequestGroup = 'RequestGroup',
}

export enum IInsomniaRequestMethod {
  GET = 'GET',
  POST = 'POST',
}

export enum IInsomniaExportType {
  Export = 'export',
  Workspace = 'workspace',
  Request = 'request',
  RequestGroup = 'request_group',
  Environment = 'environment',
  Cookie = 'cookie_jar',
  APISpec = 'api_spec',
}
